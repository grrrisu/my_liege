defmodule MyLiege.Game.Board do
  def create("test") do
    %{workplaces: [], pawn_pool: %{}, inventory: %{}}
  end

  def create("one") do
    %{workplaces: [], pawn_pool: %{}, inventory: %{}}
  end
end
